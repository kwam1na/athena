# Athena Webapp Testing

Use the package commands in this guide for focused iteration and diagnosis. Once a branch is merge-ready, run `bun run pr:athena` from the repository root before any independently assembled broad suite. Its ordering intentionally checks inexpensive prerequisites such as delivery documentation before coverage and other expensive validation, then records proof for pre-push reuse.

Use the repo-root harness commands together:

- `bun run harness:check` validates the docs themselves: required files, links, path references, and documented test commands.
- `bun run harness:review` is the touched-file pass. It always runs `bun run harness:check` first, then uses the machine-readable [validation map](./validation-map.json) to decide whether `@athena/webapp` baseline validations and mapped runtime behavior scenarios should run for the files you changed.
- `bun run harness:audit` is the full-app pass. It scans the current `athena-webapp` surface even when nothing is touched and fails on stale harness docs, stale validation-map paths, or live surfaces that the validation map does not cover yet.
- `bun run harness:inferential-review` is the blocking inferential pass. It emits human-readable remediation plus a machine-readable inferential review artifact in the repo-level artifacts directory.
- `bun run harness:behavior --scenario <name>` runs shared runtime behavior scenarios that boot app processes, wait for readiness, drive browser interactions, assert runtime signals, and clean up automatically.
- `bun run harness:behavior --scenario <name> --record-video` captures browser-flow evidence for handoff under `artifacts/harness-behavior/videos/<scenario>/<run-stamp>/`.
- `bun run --filter '@athena/webapp' test:e2e` builds the Athena webapp, serves the production build with Vite preview, and runs package-local Playwright specs.
- `bun run --filter '@athena/webapp' test:e2e -- <spec>` limits that Playwright run to a specific spec such as `src/tests/pos/offlineRouteAccess.spec.ts`.

Behavior runs emit `[harness:behavior:report]` JSON with per-phase latency and runtime-signal diagnostics. Thresholds are configured per scenario through `runtimeSignals[].minMatches` / `runtimeSignals[].maxMatches` and `thresholds.latency` in [scripts/harness-behavior-scenarios.ts](../../../../scripts/harness-behavior-scenarios.ts).

## Timing parity with the CI coverage run

CI validates this package under v8 coverage instrumentation (`test:coverage`), where a full run takes roughly eighteen minutes. Scheduled callbacks land much later relative to promise/microtask work than they do on an idle laptop, so a plain local `bun run test` can be green on a tree CI rejects. Any sensor CI runs that the local gate cannot is a parity gap.

`bun run --filter '@athena/webapp' test:timing-parity` closes it for the class of failure that skew actually produces. It re-runs the POS suites with every scheduled delay multiplied by `ATHENA_TEST_TIMER_LAG` (default 10), which stretches timer-driven polling — including Testing Library's `waitFor` — while promise chains keep running at full speed. Timeouts scale with the lag, so honest waits do not turn into spurious failures. Override the surface with `ATHENA_TIMING_PARITY_PATHS` and the factor with `ATHENA_TEST_TIMER_LAG`; the shim lives in [vitest.setup.ts](../../vitest.setup.ts) and is inert unless the variable is set.

The rule it enforces: **`await waitFor(<positive>)` followed by `expect(...).not.toHaveBeenCalled()` is unsound whenever the awaited work can also trigger the negative.** `waitFor` resolves on the first poll that passes, and the work it awaited keeps advancing while the negative assertion runs. Wait for the settled state instead — a signal that proves the work in question finished (a retry issued, a status published) — or freeze the state under test so the negative cannot be beaten. Freezing is usually the cleaner fix: let the first attempt fail and park every retry in flight, so an escalation that needs several consecutive failures can never fire behind the assertion. Both POS local-sync retry tests in [usePosLocalSyncRuntime.test.ts](../../src/lib/pos/infrastructure/local/usePosLocalSyncRuntime.test.ts) use that shape.

- [Test index](./test-index.md)
- [Validation guide](./validation-guide.md)

If `bun run harness:review` reports a coverage gap, the touched `packages/athena-webapp` file is not represented in the validation map yet. Update the map (including `behaviorScenarios` when runtime checks are required) and this testing guide together before handoff so the harness stays honest.
The generated [validation guide](./validation-guide.md) and [validation map](./validation-map.json) are derived from [scripts/harness-app-registry.ts](../../../../scripts/harness-app-registry.ts); update that registry metadata and rerun `bun run harness:generate` instead of editing the generated files by hand.

If `bun run harness:audit` reports a coverage gap, a live `src/` or `convex/` surface exists without a corresponding validation-map entry. Add or tighten the affected surface mapping before handoff so future agents can trust the repo-wide scan.

If `bun run harness:inferential-review` reports findings, treat them as blocking: the command exits non-zero and includes remediation guidance for each actionable issue. If it reports a provider/runtime error, resolve the error and rerun before handoff.

Use `bun run harness:behavior --list` to inspect available runtime scenarios.
Current shared scenarios include:
- `sample-runtime-smoke` (shared contract smoke check)
- `athena-admin-shell-boot`
- `athena-convex-storefront-composition`
- `athena-convex-storefront-failure-visibility`
- `athena-qa-live-smoke`
- `valkey-proxy-local-request-response`
- `storefront-backend-first-load`
- `storefront-checkout-bootstrap`
- `storefront-checkout-validation-blocker`
- `storefront-checkout-verification-recovery`

Shared-demo product image assets under `assets/shared-demo-products` use the
shared-demo validation slice so asset changes remain covered by the same seed,
restore, frontend, typecheck, and build checks as the demo runtime.

Convex artifact refresh rule:

- If a change needs new `convex/_generated` artifacts or refreshed client refs, start `bunx convex dev` from `packages/athena-webapp` before validation.
- Do not substitute `bunx convex codegen` for this workflow; local agent worktrees may not have `CONVEX_DEPLOYMENT` configured, and `convex dev` is the documented artifact-refresh path in this repo.

## Local dev POS cashier

For localhost and development live testing only, sign into Athena with the development account below, then open its seeded store and use the provisioned POS cashier:

- Athena user: `knownothing955@gmail.com`
- Cashier username: `jdoog`, PIN: `1111` (register sign-in, drawer open/closeout, sales, void *requests*)
- Manager username: `kmens`, PIN: `1111` (manager paths: unlock the Approvals queue, approve/reject void and other manager-approved commands)

Retrieve the one-time login code from that account's Gmail inbox when needed; never copy one-time codes into repository files. Do not use these credentials against production. If the cashier is missing or the PIN no longer works in development, provision or reset the cashier through Staff Management rather than adding a test-only authentication bypass.

Start with the package suite in [vitest.config.ts](../../vitest.config.ts): `bun run --filter '@athena/webapp' test`. It covers `src/**/*.test.{ts,tsx}`, `convex/**/*.test.{ts,tsx}`, and `shared/**/*.test.{ts,tsx}`, so it is the default regression pass for mixed UI and backend changes.

Client/server error-foundation rule:

- Use [shared/commandResult.ts](../../shared/commandResult.ts) for browser-safe `ok` / `user_error` results that both Convex and React can import.
- Approval-sensitive commands should use [shared/approvalPolicy.ts](../../shared/approvalPolicy.ts), [convex/operations/approvalProofs.ts](../../convex/operations/approvalProofs.ts), [convex/operations/approvalAuditEvents.ts](../../convex/operations/approvalAuditEvents.ts), and [src/components/operations/CommandApprovalDialog.tsx](../../src/components/operations/CommandApprovalDialog.tsx). Tests should prove the first command attempt returns `approval_required`, no protected mutation happens before proof consumption, a matching proof allows the same command path to proceed, and mismatched/expired/consumed proofs fail safely.
- Browser code must normalize command execution through [src/lib/errors/runCommand.ts](../../src/lib/errors/runCommand.ts), [src/lib/errors/presentCommandToast.ts](../../src/lib/errors/presentCommandToast.ts), and [src/lib/errors/presentUnexpectedErrorToast.ts](../../src/lib/errors/presentUnexpectedErrorToast.ts) instead of rendering arbitrary thrown text.
- Durable surfaces render command failures inline by default. Toast is the fallback only when the action has no durable error region.
- Keep shared error helpers browser-safe; [src/routeTree.browser-boundary.test.ts](../../src/routeTree.browser-boundary.test.ts) is the regression guard against importing raw Convex server modules into the browser tree.

Escalate validation based on the surface you touched:

- Reporting contract, ingress, source-adapter, fact recognition, inventory valuation, projection, migration, public-read, health, export, or activation changes: run `bun run --filter '@athena/webapp' test -- convex/reporting` before broader package validation. The focused suite must prove idempotent replay and sanitized conflict handling, unknown-versus-zero valuation, deficit and moving-average invariants, incremental-versus-rebuild projection parity, backfill preview/pause/resume/retry behavior, store-scoped full-admin access, bounded evidence, compatible verified-generation activation and rollback, and explicit stale/failed/partial health. Pair this with the owning source-domain tests whenever POS, storefront, service, payment, inventory, procurement, or Daily Close commands emit or change ingress/effects; prove source transition, ingress scheduling, and inline inventory effects commit atomically, while canonicalization and projection failure leave accepted operational success intact. Before store activation, extend the focused suite with scale and deployment release gates for supported cardinality, p95 read targets, shadow drift, lag, version compatibility, and rollback. Then run `bun run --filter '@athena/webapp' audit:convex`, `bun run --filter '@athena/webapp' lint:convex:changed`, `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json`, `bun run graphify:rebuild`, and `bun run --filter '@athena/webapp' build`.
- Workflow trace foundation, POS local sync/register infrastructure, POS register bootstrap or drawer-gate changes, `pos_session` / `register_session` write paths, or POS trace entry-point changes: run `bun run --filter '@athena/webapp' test -- convex/workflowTraces/presentation.test.ts convex/workflowTraces/queryUsage.test.ts convex/workflowTraces/schemaIndexes.test.ts convex/workflowTraces/adapters/posSession.test.ts convex/workflowTraces/adapters/registerSession.test.ts convex/pos/application/completeTransaction.test.ts convex/pos/application/getTransactions.test.ts convex/pos/application/posSessionTracing.test.ts convex/pos/application/terminals.test.ts convex/pos/application/sync/ingestLocalEvents.test.ts convex/pos/application/sync/projectLocalEvents.test.ts convex/pos/public/sync.test.ts convex/pos/public/terminals.test.ts convex/inventory/posSessions.trace.test.ts convex/operations/registerSessionTracing.test.ts convex/operations/registerSessions.trace.test.ts convex/cashControls/registerSessionTraceLifecycle.test.ts src/components/traces/WorkflowTraceView.test.tsx 'src/routes/_authed/$orgUrlSlug/store/$storeUrlSlug/traces/$traceId.test.tsx' src/components/pos/transactions/transactionColumns.test.tsx src/components/pos/transactions/TransactionView.test.tsx src/components/pos/transactions/TransactionsView.test.tsx src/components/pos/SessionManager.test.tsx src/components/pos/register/POSRegisterView.test.tsx src/components/pos/session/HeldSessionsList.test.tsx src/lib/pos/infrastructure/local/posLocalStore.test.ts src/lib/pos/infrastructure/local/syncContract.test.ts src/lib/pos/infrastructure/local/syncScheduler.test.ts src/lib/pos/infrastructure/local/syncStatus.test.ts src/lib/pos/infrastructure/local/usePosLocalSyncRuntime.test.ts src/lib/pos/infrastructure/local/registerReadModel.test.ts src/lib/pos/infrastructure/local/localCommandGateway.test.ts src/lib/pos/presentation/register/useRegisterViewModel.test.ts src/components/cash-controls/CashControlsDashboard.test.tsx src/components/cash-controls/RegisterSessionView.test.tsx src/lib/traces/createWorkflowTraceId.test.ts` before broader package validation. Then run `bun run --filter '@athena/webapp' audit:convex`, `bun run --filter '@athena/webapp' lint:convex:changed`, `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json`, and `bun run --filter '@athena/webapp' build`. When the shared route or Convex-backed presentation path changes, also run the `athena-admin-shell-boot`, `athena-convex-storefront-composition`, and `athena-convex-storefront-failure-visibility` behavior scenarios through `bun run harness:behavior`.
- POS service mixed-checkout receipts, read models, service-case payment context, service catalog local snapshot/readiness, mixed-sale void guardrails, daily close, or cash-control allocation reporting changes: run `bun run --filter '@athena/webapp' test -- convex/pos/application/completeTransaction.test.ts convex/pos/application/getTransactions.test.ts convex/pos/public/transactions.test.ts convex/operations/dailyClose.test.ts convex/cashControls/registerSessions.test.ts src/components/pos/OrderSummary.test.tsx src/components/pos/register/RegisterCheckoutPanel.test.tsx src/components/pos/receipt/PosReceiptShareControl.test.tsx src/components/pos/transactions/TransactionView.test.tsx src/components/pos/transactions/TransactionsView.test.tsx src/components/services/ServiceCasesView.test.tsx src/components/operations/CommandApprovalDialog.test.tsx src/lib/pos/infrastructure/local/localPosReadiness.test.ts src/lib/pos/infrastructure/local/posLocalStore.test.ts src/lib/pos/infrastructure/local/usePosLocalSyncRuntime.test.ts` before broader package validation. Mixed checkout must keep service payments explicit, keep product add-ons on retail inventory paths, leave service material usage out of POS, and count drawer tender once even when payment allocations split. Then run `bun run --filter '@athena/webapp' audit:convex`, `bun run --filter '@athena/webapp' lint:convex:changed`, `bun run --filter '@athena/webapp' lint:frontend:changed`, `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json`, `bun run graphify:rebuild`, and `bun run --filter '@athena/webapp' build`.
- Client/server error foundation, generic boundary, or browser-safe shared command contract changes: run `bun run --filter '@athena/webapp' test -- shared/commandResult.test.ts src/lib/errors/runCommand.test.ts src/lib/errors/presentCommandToast.test.ts src/lib/errors/presentUnexpectedErrorToast.test.ts src/components/auth/DefaultCatchBoundary.test.tsx src/routeTree.browser-boundary.test.ts` before broader package validation. Then run `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json` and `bun run --filter '@athena/webapp' build`.
- Command approval policy, approval proof, approval presenter, transaction payment correction, or variance review changes: run `bun run --filter '@athena/webapp' test -- shared/commandResult.test.ts convex/lib/commandResultValidators.test.ts convex/operations/approvalProofs.test.ts convex/operations/approvalAuditEvents.test.ts convex/operations/staffCredentials.test.ts convex/pos/application/correctTransactionPaymentMethod.test.ts convex/pos/public/transactions.test.ts src/lib/errors/runCommand.test.ts src/lib/errors/presentCommandToast.test.ts src/components/operations/CommandApprovalDialog.test.tsx src/components/pos/transactions/TransactionView.test.tsx convex/cashControls/closeouts.test.ts src/components/cash-controls/RegisterSessionView.test.tsx src/components/cash-controls/RegisterSessionView.auth.test.tsx` before broader package validation. Then run `bun run --filter '@athena/webapp' audit:convex`, `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json`, `bun run graphify:rebuild`, and `git diff --check`.
- Completed-transaction item adjustment, adjustment approval/application, transaction detail adjustment history, daily close adjusted totals, or cash-control settlement display changes: run `bun run --filter '@athena/webapp' test -- convex/operations/dailyClose.test.ts convex/operations/dailyOperations.test.ts convex/pos/application/completeTransaction.test.ts convex/pos/application/correctTransactionPaymentMethod.test.ts src/components/cash-controls/RegisterSessionView.test.tsx src/components/pos/transactions/TransactionView.test.tsx src/components/operations/OperationsQueueView.test.tsx` before broader package validation. Daily close and daily operations reports must keep original `salesTotal` / `paymentTotals` semantics and add explicit adjusted/net settlement fields; pending or rejected adjustments must stay out of those adjusted/net totals. Then run `bun run --filter '@athena/webapp' audit:convex`, `bun run --filter '@athena/webapp' lint:convex:changed`, `bun run --filter '@athena/webapp' lint:frontend:changed`, `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json`, and `bun run --filter '@athena/webapp' build`.
- Login auth sync, staff foundation, store-configuration admin, or cashier-auth command handling changes: run `bun run --filter '@athena/webapp' test -- src/routes/login/_layout.test.tsx convex/operations/staffCredentials.test.ts convex/operations/staffProfiles.test.ts convex/inventory/sessionQueryIndexes.test.ts convex/pos/application/sessionCommands.test.ts convex/pos/application/completeTransaction.test.ts convex/pos/application/getTransactions.test.ts convex/pos/infrastructure/repositories/sessionRepository.test.ts convex/pos/application/getRegisterState.test.ts convex/inventory/posSessions.trace.test.ts convex/pos/application/posSessionTracing.test.ts src/lib/pos/infrastructure/convex/sessionGateway.test.ts src/lib/pos/infrastructure/convex/registerGateway.test.ts src/components/pos/CashierAuthDialog.test.tsx src/components/pos/register/POSRegisterView.test.tsx src/lib/pos/presentation/register/useRegisterViewModel.test.ts src/components/pos/transactions/TransactionView.test.tsx src/components/staff/StaffManagement.test.tsx src/components/store-configuration/hooks/useStoreConfigUpdate.test.tsx src/components/store-configuration/components/FulfillmentView.test.tsx src/components/store-configuration/components/MaintenanceView.test.tsx src/components/store-configuration/components/MtnMomoView.test.tsx` before broader package validation. Then run `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json` and `bun run --filter '@athena/webapp' build`. This is the focused slice for retryable auth-sync behavior, staff-management provisioning edits, the shared store-configuration hook plus fulfillment/maintenance/MTN MoMo regressions, and cashier-auth flows that now normalize `CommandResult` failures through shared inline or toast-safe handling. If the change adds or removes public Convex functions, refresh `convex/_generated` via `bunx convex dev` when the workspace has deployment credentials; otherwise record the manual generated-file patch in handoff.
- Expense-session lifecycle, expense cart, or expense transaction changes: run `bun run --filter '@athena/webapp' test -- convex/inventory/expenseSessions.test.ts convex/inventory/sessionQueryIndexes.test.ts src/hooks/useExpenseSessions.test.ts` before broader package validation. Then run `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json` and `bun run --filter '@athena/webapp' build`. Reach for this when expense session mutations, cart-operation hooks like [src/hooks/useExpenseOperations.ts](../../src/hooks/useExpenseOperations.ts), or browser-facing session hooks change and you need to confirm that expected failures still collapse to safe command-result copy instead of leaking backend text.
- Expense register product search changes: run `bun run --filter '@athena/webapp' test -- src/lib/pos/presentation/expense/useExpenseRegisterViewModel.test.ts src/lib/pos/presentation/register/catalogSearch.test.ts` before broader package validation. Add `src/lib/pos/presentation/register/useRegisterViewModel.test.ts` when shared local-index helpers, availability overlays, or exact-match auto-add semantics change.
- Omnichannel order, refund, review, messaging, or customer-history changes: run `bun run --filter '@athena/webapp' test -- convex/storeFront/errorFoundation.test.ts convex/storeFront/orderOperations.test.ts convex/storeFront/returnExchangeOperations.test.ts convex/storeFront/customerBehaviorTimeline.test.ts convex/storeFront/customerObservabilityTimeline.test.ts convex/storeFront/helpers/customerEngagementEvents.test.ts src/components/orders/ReturnExchangeView.test.tsx src/components/users/TimelineEventCard.test.tsx` before broader package validation. Then run `bun run --filter '@athena/webapp' audit:convex`, `bun run --filter '@athena/webapp' lint:convex:changed`, `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json`, and `bun run --filter '@athena/webapp' build`. Reach for this when [convex/storeFront/onlineOrder.ts](../../convex/storeFront/onlineOrder.ts), [convex/storeFront/onlineOrderUtilFns.ts](../../convex/storeFront/onlineOrderUtilFns.ts), [convex/storeFront/payment.ts](../../convex/storeFront/payment.ts), [convex/storeFront/reviews.ts](../../convex/storeFront/reviews.ts), or the operator views under [src/components/orders](../../src/components/orders) and [src/components/reviews](../../src/components/reviews) change. If route-to-backend composition or browser-visible failure copy could shift, also run the `athena-convex-storefront-composition` and `athena-convex-storefront-failure-visibility` behavior scenarios.
- Admin quick-action toast-fallback edits: run `bun run --filter '@athena/webapp' test -- src/lib/errors/presentUnexpectedErrorToast.test.ts src/components/join-team/index.test.tsx src/components/promo-codes/PromoCodeHeader.test.tsx` before broader package validation. Then run `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json` and `bun run --filter '@athena/webapp' build`. Use this slice when [src/components/add-product](../../src/components/add-product), [src/components/assets](../../src/components/assets), [src/components/join-team](../../src/components/join-team), [src/components/organization-members](../../src/components/organization-members), [src/components/promo-codes](../../src/components/promo-codes), or the settings views under [src/settings](../../src/settings) change and the action still resolves through toast-only UX.
- Service operations intake, catalog, appointment, case, or manager-queue changes: run `bun run --filter '@athena/webapp' test convex/serviceOps/serviceCases.test.ts convex/serviceOps/catalogAppointments.test.ts convex/serviceOps/moduleWiring.test.ts convex/operations/serviceIntake.test.ts src/components/services/ServiceIntakeView.test.tsx src/components/services/ServiceIntakeView.auth.test.tsx src/components/services/ServiceAppointmentsView.test.tsx src/components/services/ServiceCasesView.test.tsx src/components/services/ServiceCatalogView.test.tsx src/components/operations/OperationsQueueView.test.tsx` before broader package validation. Then run `bun run --filter '@athena/webapp' audit:convex`, `bun run --filter '@athena/webapp' lint:convex:changed`, `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json`, and `bun run --filter '@athena/webapp' build` so the service-specific mutation coverage and the shared Convex rails stay in sync.
- SKU activity tracing, reservation explanation, or timeline-inspection UI changes: run `bun run --filter '@athena/webapp' test -- src/components/operations/SkuActivityTimeline.test.tsx` for the browser inspection surface. If the backend query, ledger helper, POS hold instrumentation, checkout reservation instrumentation, or committed movement linkage changes in the same branch, pair it with the focused Convex tests for those files and then run `bun run --filter '@athena/webapp' audit:convex`, `bun run --filter '@athena/webapp' lint:convex:changed`, `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json`, and `bun run --filter '@athena/webapp' build`. The UI test must keep safe loading, empty, failure, active POS reservation, active checkout reservation, and unexplained availability-gap states covered.
- Stock adjustments, purchase orders, replenishment, receiving, or procurement route changes: run `bun run --filter '@athena/webapp' test convex/stockOps/access.test.ts convex/stockOps/adjustments.test.ts convex/stockOps/purchaseOrders.test.ts convex/stockOps/receiving.test.ts convex/stockOps/replenishment.test.ts convex/stockOps/vendors.test.ts src/components/operations/StockAdjustmentWorkspace.test.tsx src/components/operations/OperationsQueueView.test.tsx src/components/procurement/ProcurementView.test.tsx src/components/procurement/ReceivingView.test.tsx` before broader package validation. Then run `bun run --filter '@athena/webapp' audit:convex` and `bun run --filter '@athena/webapp' lint:convex:changed` before PR handoff because these flows cross inventory mutations, approvals, and store-scoped auth gates. If the procurement route tree, sidebar wiring, or generated Convex client refs change, start `bunx convex dev` from `packages/athena-webapp`, then follow with `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json` and `bun run --filter '@athena/webapp' build`.
- Cash-controls register-session, deposit, closeout, or dashboard changes: run `bun run --filter '@athena/webapp' test convex/cashControls/registerSessions.test.ts convex/cashControls/closeouts.test.ts convex/cashControls/deposits.test.ts src/components/cash-controls/CashControlsDashboard.test.tsx src/components/cash-controls/RegisterSessionView.test.tsx src/components/operations/OperationsQueueView.test.tsx` before broader package validation. Treat this as the confirmation slice that drawers opened from POS are visible in the dashboard and register-session views. If the cash-controls route tree, sidebar wiring, or generated Convex client refs change, start `bunx convex dev` from `packages/athena-webapp`, then follow with `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json` and `bun run --filter '@athena/webapp' build`.
- New service routes or sidebar wiring: regenerate owned artifacts instead of editing them by hand, then run `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json` and `bun run --filter '@athena/webapp' build`.
- Convex HTTP composition or auth-route changes: run targeted tests like [convex/http/routerComposition.test.ts](../../convex/http/routerComposition.test.ts) and [convex/http/domains/customerChannel/routes/security.test.ts](../../convex/http/domains/customerChannel/routes/security.test.ts).
- Inventory query or POS behavior changes outside the drawer-gate or trace contract: spot-check existing guards such as [convex/inventory/posQueryCleanup.test.ts](../../convex/inventory/posQueryCleanup.test.ts) and [src/tests/pos/usePrint.test.ts](../../src/tests/pos/usePrint.test.ts). If the change touched POS register bootstrap or drawer opening, pair those spot checks with [src/components/pos/register/POSRegisterView.test.tsx](../../src/components/pos/register/POSRegisterView.test.tsx), [src/lib/pos/presentation/register/useRegisterViewModel.test.ts](../../src/lib/pos/presentation/register/useRegisterViewModel.test.ts), [src/components/cash-controls/CashControlsDashboard.test.tsx](../../src/components/cash-controls/CashControlsDashboard.test.tsx), and [src/components/cash-controls/RegisterSessionView.test.tsx](../../src/components/cash-controls/RegisterSessionView.test.tsx).
- Authenticated admin/store route-boundary changes: run `bun run --filter '@athena/webapp' lint:architecture` to catch lower-layer imports that reach back into `_authed` route or shell entrypoints.
- Any public Convex function or Hono route added, removed, or re-wrapped: `bun scripts/convex-operation-admission-check.ts` from the repository root is the coverage sensor for the operation admission rail, and it must exit 0 with zero findings. Add `--path <prefix>` to restrict it to the convex-relative prefixes you touched while iterating. [convex/operationAdmission/coverage.test.ts](../../convex/operationAdmission/coverage.test.ts) is the zero-findings assertion inside the package suite, so a new unadmitted export fails the suite as well as the checker. Test the **exported** handler, not the `*WithCtx` helper: the exported symbol is the one the canonical wrapper (`admitPublicMutation`, `admitPublicQuery`, `admitPublicAction`, `admitHttpRoute`, `admitHttpRead`) is applied to, so a test that calls the helper directly proves the domain logic while skipping the admission boundary it is meant to cover. Assert the operation definition too — definitions are exported by name from [convex/operationAdmission/domains](../../convex/operationAdmission/domains) precisely so tests can bind to them instead of searching the registry. Then run `bun run --filter '@athena/webapp' test -- convex/operationAdmission convex/sharedDemo convex/platform convex/http` alongside the owning domain's own suite. The contract these tests hold to is [convex/operationAdmission/README.md](../../convex/operationAdmission/README.md).
- Agent harness kernel, a domain capability package, a profile, the delegated admission ports, or the reusable agent host: run `bun run --filter '@athena/webapp' test -- convex/agentHarness shared/agentHarness convex/operationAdmission convex/platform src/components/agent` before broader package validation. Three checks gate this surface and none may be skipped. **Generated registry drift**: `bun run agent-sdk:check` must report the artifacts fresh; it refuses a stale one and names `bun run agent-sdk:generate`. **Admission coverage**: [convex/operationAdmission/coverage.test.ts](../../convex/operationAdmission/coverage.test.ts) must report zero findings and a current caller table, and it also asserts that the only agent surface on the ingress rail is the operator turn and that every published read port is a bound internal query. **Conformance, security, and data governance**: [convex/agentHarness/releaseConformance.test.ts](../../convex/agentHarness/releaseConformance.test.ts) runs the enable gate over every registered manifest, [convex/agentHarness/security.test.ts](../../convex/agentHarness/security.test.ts) points the adversary at the published composition (guessed and cross-store references, forged citations, revoked authority, disabled capabilities, sandbox escape, prompt injection through product data, tool-protocol replay, usage settlement), [convex/agentHarness/dataGovernance.test.ts](../../convex/agentHarness/dataGovernance.test.ts) follows one cited answer through retention and the deletion cascade, and the deterministic smoke matrix in [convex/agentHarness/evals](../../convex/agentHarness/evals) runs model-shaped programs through the real sandbox and the real read ports. Then run `bun run --filter '@athena/webapp' audit:convex`, `bun run --filter '@athena/webapp' lint:convex:changed`, `bun run --filter '@athena/webapp' lint:frontend:changed`, `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json`, and `bun run --filter '@athena/webapp' build`. If a read port's behaviour changed, bump its `implementationVersion` on both the manifest binding and the port definition — the generator refuses a changed source under an unchanged version — and run the pre-deploy fence (`bun scripts/agent-harness-fence.ts --reason "<id>"`, `--dry-run` to preview) against the deployment about to be replaced before the change ships. Invoke that script and `bun scripts/agent-harness-switch.ts` directly rather than through their `bun run agent-harness:*` aliases — `bun run <script> -- --reason "a b c"` re-splits the quoted value into three arguments. The fence is one atomic transaction, not a phase: it disables the named profiles and advances the durable epoch so old-digest runs are terminally invalidated rather than drained. One limit to state plainly: `convex-test` executes sequentially, so nothing in this slice proves concurrent behaviour. Double reservation, double completion, and racing cancellation rest on idempotency keys, compare-and-set patches, and Convex serializability — reviewed, not raced. The authoring contract is [capability-authoring.md](./capability-authoring.md). Two durable notes explain why the sensor list above is ordered the way it is and what each rung cannot prove: [the sensor ladder](../../../../docs/solutions/harness/the-sensor-ladder-what-a-green-suite-cannot-see-2026-08-22.md) and [Convex deploy and module-graph constraints](../../../../docs/solutions/harness/convex-deploy-and-module-graph-constraints-2026-08-22.md).
- Provisional narrative streaming — the runtime contract event, the adapter's text stream, the turn host's flush, the preview query, or the host's provisional UI: run `bun run --filter '@athena/webapp' test -- shared/agentHarness/contracts.test.ts shared/agentHarness/agentRuntime.contract.test.ts convex/agentHarness/agentRuntime/convexAgent.contract.test.ts convex/agentHarness/agentRuntime/convexAgentPersistence.test.ts convex/agentHarness/provisionalNarrative.test.ts convex/agentHarness/turns.test.ts convex/agentHarness/runtimeHost.test.ts convex/agentHarness/retention.test.ts convex/agentHarness/security.test.ts convex/agentHarness/importBoundary.test.ts convex/operationAdmission src/components/agent src/tests/agent/provisionalWithdrawalReasons.test.ts` before broader package validation, then the scripted browser pass `bun run --filter '@athena/webapp' test:e2e -- src/tests/agent/athenaAgentHost.spec.ts`. This is the full agent-harness slice above narrowed to one contract, so run that slice too whenever the change is not confined to streaming. What each rung is for: the contract suites prove `narrative_delta` validation and that both the fake and the real adapter emit the same sequence; the adapter tests prove the text-part allowlist, the coalescing rule, and that the component still persists no stream rows; `convex/agentHarness/provisionalNarrative.test.ts` and `convex/agentHarness/turns.test.ts` prove the delete-only row, the flush's refusal ladder, and every arm of the preview union including its executable `returns` conformance; `convex/agentHarness/runtimeHost.test.ts` holds the amended invariant — the narrative never enters Athena's durable record and never survives a terminal cause; `convex/agentHarness/retention.test.ts` proves the repair sweep's expiry deletion and its log line; `convex/agentHarness/security.test.ts` points the adversary at the flush, the preview, and the acknowledgement, and asserts the flush is internal-only while the two operator entry points are admitted; `convex/agentHarness/importBoundary.test.ts` keeps the leaf helper a leaf and keeps runtime-native identifiers out of `shared/` and `src/`; `convex/operationAdmission` is the coverage assertion for the two new public functions; `src/tests/agent/provisionalWithdrawalReasons.test.ts` pins the host's withdrawal vocabulary to the server's closed set, and it lives outside `src/components/agent` precisely so it may hold both lists at once. Then run `bun run --filter '@athena/webapp' audit:convex`, `bun run --filter '@athena/webapp' lint:convex:changed`, `bun run --filter '@athena/webapp' lint:frontend:changed`, `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json`, and `bun run --filter '@athena/webapp' build`. Two things this slice cannot prove and no test in it should be read as proving: `convex-test` is sequential, so ordering between a flush and a concurrent turn writer rests on single-flight construction and Convex serializability rather than on a raced test; and none of it exercises a real provider, so time-to-first-provisional-text is read from the `[agentHarness:driveTurn]` log line on a deployment (`bunx convex logs --history 200`), not from a green suite. Changing a profile's `narrativePolicy` moves the compatibility digest and therefore needs the pre-deploy fence, exactly like a read-port behaviour change; [agent-harness-runtime.md](./agent-harness-runtime.md) section 5 carries that posture and how to read both log lines.
- Agent harness on a deployment: `bunx convex run agentHarness/deploymentState:describeDeploymentState '{}'` reports the epoch, the deployed digest, and every profile's published baseline versus effective switch state. `bunx convex run agentHarness/evals/dailyOperations:runSmoke '{"organizationSlug":"<org>","storeSlug":"<store>","operatorEmail":"<operator>","operatingDate":"<YYYY-MM-DD>"}'` runs the same six-scenario matrix against the real store while the operator switch is still off. That is a **direct-harness smoke, not an operator turn**: it composes its own admission over the real manifests, ports, registry, executor, sandbox, budgets, and evidence, but registers every published profile `enabled` and dispatches through its own port wrappers, so it creates no turn binding, releases no answer, and terminalizes every run it starts. An operator/profile turn is the opposite: it starts at the operation-admitted public entry points in [convex/agentHarness/turns.ts](../../convex/agentHarness/turns.ts), and a switched-off profile denies it `profile_disabled`. Capability kill switches are read live from the durable overlay in both. The release order is publish → deploy → fence if a port's behaviour moved → smoke while off → one enable → watch the first turns → disable to roll back; there are no cohorts, canaries, staged profile versions, or drain gates. `bun scripts/agent-harness-switch.ts --profile <id> --enable --reason "<id>"` enables broadly and `--disable` is the rollback. When a turn misbehaves, `bunx convex run agentHarness/evals/directHarness:describeRunDiagnostics '{"runId":"<id>"}'` prints the run status, budget charged, each attempt's validator diagnostic, each capability call's refusal, and the authored program text.
- Any change under `convex/`: run `bun run --filter '@athena/webapp' audit:convex` and `bun run --filter '@athena/webapp' lint:convex:changed` before PR handoff.
- Route, typing, or build-pipeline changes: run `bunx tsc --noEmit -p packages/athena-webapp/tsconfig.json` or `bun run --filter '@athena/webapp' build`.

Avoid editing generated files like [src/routeTree.gen.ts](../../src/routeTree.gen.ts) or anything under `convex/_generated`; regenerate instead if a tool owns them.
