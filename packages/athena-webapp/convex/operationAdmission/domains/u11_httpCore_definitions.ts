import type { OperationDefinition } from "../types";

/**
 * U11 - HTTP core, messaging and money-movement routes - write/action/http operation definitions.
 *
 * Scaffolded by U1a so the composing arrays in `definitions.ts` never need to
 * change again: the owning unit fills this array and edits nothing else.
 */
export const U11_HTTP_CORE_OPERATION_DEFINITIONS: readonly OperationDefinition[] = [];
