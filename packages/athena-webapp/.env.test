# Test env: pin the walkthrough privacy contact empty so the privacy
# prelaunch-gate tests assert the committed (contact-unset) default regardless
# of a developer's local .env.local. `.env.test` (mode "test") is loaded with
# higher precedence than `.env.local`, and dev (mode "development") is unaffected.
VITE_WALKTHROUGH_PRIVACY_CONTACT=
