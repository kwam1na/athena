/**
 * `@athena/contracts` — the package-neutral contract surface shared by
 * `@athena/webapp` (admin/POS) and `@athena/storefront-webapp`.
 *
 * See ./README.md for ownership rules and how to add a contract.
 */
export type { Address, Doc, Id } from "./convexModel";
export * from "./models";
export * from "./storeConfig";
