/**
 * Browser-safe intelligence context-tracking contract.
 *
 * The implementation is owned by `packages/athena-webapp/shared/intelligence`;
 * this module is the neutral specifier storefront code imports so it never has
 * to name the Athena webapp package.
 */
export * from "../athena-webapp/shared/intelligence";
