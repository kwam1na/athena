/**
 * Browser-safe homepage ranking contract.
 *
 * The implementation is owned by
 * `packages/athena-webapp/shared/homepageRanking`; this module is the neutral
 * specifier storefront code imports so it never has to name the Athena webapp
 * package.
 */
export * from "../athena-webapp/shared/homepageRanking";
