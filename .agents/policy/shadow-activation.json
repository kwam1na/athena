{
  "spec": "athena-shadow-activation/1",
  "repositoryId": "athena",
  "recordedAt": "2026-08-30T22:10:00Z",
  "purpose": "Athena's activation metadata for the read-only shadow window: the composed delivery product is installed and its run-pinned projection is materialized into managed delivery worktrees, while `bun run pr:athena` remains the repository's only delivery authority. The discovery-scoping mechanism itself is product-owned; this document carries only Athena's activation of it and the positions the exactly-one-discovery guard enforces.",
  "installationMode": "shadow",
  "deliveryAuthority": "none",
  "comparisonAuthority": "bun run pr:athena",
  "product": {
    "repository": "agent-delivery-harness",
    "commit": "8635ea8aca18f27f660b3551b950ffb7e6ad22dd",
    "compositionProfile": "production",
    "compositionProfileNote": "the fixture profile registers deliveries only in the disposable repositories its install receipt names, so an installation serving repository athena must carry the production profile; the read-only posture comes from this document's shadow installation mode, not from the profile",
    "qualificationRecords": [
      "qualifications/claude-code-integration.json",
      "qualifications/host-admission-capabilities.json",
      "qualifications/manual-choreography-baseline.json"
    ],
    "installationPaths": {
      "rule": "disposable, operator-chosen directories created for the shadow window only",
      "prohibited": [
        "the operator's real Claude Code configuration directory",
        "the operator's real Codex configuration directory",
        "any path inside the Athena working tree"
      ]
    }
  },
  "projection": {
    "root": ".managed-projection",
    "owner": "the product's Claude Code binding (materialization, digest receipt, per-run consumption marker, teardown)",
    "managedDeliveryWorktreeRoot": ".worktrees/managed",
    "scopeRule": "the projection root may exist only inside a managed delivery worktree; the repository root and every non-managed worktree must not carry one",
    "managedDeliveryWorktreePathShape": "the operator creates each managed delivery worktree at .worktrees/managed/<deliveryId> — a sibling path such as .worktrees/managed-<deliveryId> is not a managed delivery worktree and a projection there is a finding",
    "mechanism": "model-external and byte-neutral — the binding writes its session settings and the worktree-scoped excludes file into its own directory outside the working tree, points the managed worktree's worktree-scoped git configuration at them, and enables worktree configuration once in the shared repository config; no tracked byte of the vendored discovery layout is moved or edited, and the configuration takes effect in the managed worktree alone",
    "whatIsNotSuppressed": "on the exclusivity-ungraded hosts of this window nothing suppresses the ambient vendored generation — it stays discoverable inside a managed worktree alongside the projection. Scoping discovery to the run-pinned projection is where this mechanism would apply on an exclusivity-graded host; until a host is graded, the honest statement is coexistence, not suppression"
  },
  "provingHost": "claude-code",
  "hosts": [
    {
      "hostId": "claude-code",
      "hostVersion": "2.1.97",
      "exclusivityGrading": "exclusivity-ungraded",
      "capability": "the host can only add skill roots; no supported configuration scopes discovery to the run-pinned projection, so ambient vendored discovery coexists inside a managed worktree",
      "gradeSource": "agent-delivery-harness qualifications/host-admission-capabilities.json (discoveryScopingExclusivity)"
    },
    {
      "hostId": "codex-cli",
      "hostVersion": "0.147.0",
      "exclusivityGrading": "exclusivity-ungraded",
      "capability": "additive extra roots only; listed so the downgrade position is not read as claude-code-specific",
      "gradeSource": "agent-delivery-harness qualifications/host-admission-capabilities.json (discoveryScopingExclusivity)"
    }
  ],
  "exclusivityPosition": {
    "duringShadowWindow": "non-blocking",
    "reason": "both graded hosts are exclusivity-ungraded, and coexisting ambient vendored discovery cannot corrupt authority while the shadow window holds none",
    "becomesBlockingAt": "the cutover's removal gate, which ends ambient vendored discovery repository-wide",
    "beforeThatGate": "no tracked byte of the vendored discovery layout changes"
  },
  "consumptionRecord": {
    "requiredSource": "binding",
    "producedBy": "the per-run marker the binding injects at projection materialization and reads back from fence-bound submissions; the marker carries the delivery, the fence, and the consumed generation archive, and the gate record's entry carries those fields plus the receipted projection digest",
    "agentSuppliedClaims": "rejected — a delivery carrying one is excluded from the comparison set and the guard reports a finding",
    "absentOrNegative": "excluded from the comparison set",
    "writtenInto": ".agents/policy/shadow-milestone-gate-record.json",
    "whatTheGuardChecks": "the record's declared source and its shape — the guard reads the gate-record artifact and cannot itself verify that the bytes came from the binding",
    "whatProvenanceRestsOn": "two things outside the guard: .agents is an additionally protected path in every checkpoint grant of .agents/policy/repository-policy.json, so no execution grant may write the gate record; and the binding-side writer that emits the entry. That writer is not present in the product at the pinned commit, so until it ships the entry has no admissible producer and the comparison set stays empty — which is the correct outcome, not a gap the guard papers over"
  },
  "vendoredDiscoveryLayout": {
    "owner": "scripts/shadow-discovery-guard.ts",
    "note": "the guard owns the layout's path set and its pinned byte digest; they are not duplicated here so there is one place to change and one place to review"
  },
  "characterization": {
    "method": "a disposable shadow install of the composed product followed by one projection materialization into a managed delivery worktree, driven through the product's own installer and Claude Code binding; no Athena byte and no host configuration outside the disposable directories was touched",
    "compositionProfile": "confirmation-fixture, with the disposable repository identity athena-shadow-characterization — the characterization ran in a scratch repository, so it used the qualification profile rather than the production profile the real Athena installation carries",
    "observedAt": "2026-08-30T22:02:00Z",
    "observed": {
      "generationDigest": "29e56eb637a54a42882825dabbff1f0fcd27594b887b6db1ac587d146940c4f3",
      "installationId": "install-646341843f375857f12689e020c05ab3",
      "projectionDigest": "6791217c94ce2cafe5aa802c057ad4db11d874920b68fbf9598bb1f86e34539e",
      "managedWorktree": {
        "projectionPresent": true,
        "gitStatusPorcelain": "",
        "consumptionMarker": {
          "deliveryId": "athena-shadow-characterization-1",
          "fence": 1,
          "consumed": "skills/agent-skills-core-v1.zip"
        }
      },
      "repositoryRoot": {
        "projectionPresent": false,
        "consumptionMarkerRead": "consumption_marker_missing",
        "trackedTreeUnchanged": true,
        "vendoredGenerationStillAuthoritative": true
      },
      "nonManagedWorktree": {
        "projectionPresent": false,
        "consumptionMarkerRead": "consumption_marker_missing"
      }
    },
    "conclusion": "the projection landed only in the managed delivery worktree, was invisible to git there, left the repository root and the non-managed worktree untouched, and produced a consumption record the binding — not the session — authored"
  }
}
