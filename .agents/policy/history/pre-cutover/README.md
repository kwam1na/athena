# Historical pre-cutover policy

These immutable files document the former shadow comparison. They have no runtime authority. Active ordinary delivery policy is declared by harness.config.ts and the current repository-policy.json; the installed product validates it. Restricted managed grants retain their original boundaries.
