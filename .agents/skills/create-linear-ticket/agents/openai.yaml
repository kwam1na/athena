interface:
  display_name: "Create Linear Ticket"
  short_description: "Generate atomic Linear tickets with test posture, expected sensors, and compounding hooks"
  default_prompt: "Use $create-linear-ticket to resolve team/project context from the issue, explicit user input, or the current working directory and then create atomic, parallelizable tickets with execution posture, test scenarios, expected sensors, and compounding opportunities."
